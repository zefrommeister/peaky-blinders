"use client";

import AnimatedBackground from '../components/AnimatedBackground';

export default function MovieCountdown() {
    return (
    <AnimatedBackground>
      <div>
        <h1>Movie Countdown</h1>
        <p>This page will contain the movie countdown information.</p>
      </div>
    </AnimatedBackground>
    );
  }